<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/holo_blue_light"
    android:clickable="true"
    android:focusable="true"
    tools:context=".datafromactivitytofragmentwithfactorymethod.CustomBlankFragment">
    <!-- The attribute android:clickable="true" so that we can't click any
    buttons or views from our parent activity when the fragment is open. Also,
    the attribute android:focusable="true" comes complimentary with the above
    attribute -->

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/text_view_custom_blank_fragment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ems="10"
        android:text="Custom Blank Fragment Text"
        android:textAlignment="center"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>